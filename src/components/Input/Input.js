import React from 'react';

import { Button } from '@material-ui/core';

export default function Input(props) {
  return (
    <div>
      <Button size="medium" variant="contained">Add Card</Button>
    </div>
  );
}
